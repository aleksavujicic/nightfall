image: maven:3.3.9-jdk-8
cache:
  paths:
    - .m2/repository/
#before_script:
#  - apt-get update -y && apt-get upgrade -y
    
common_build:
  script:
    - mvn install -B
  artifacts:
    name: "Nightfall-Common"
    paths:
      - ./Nightfall-Common.jar